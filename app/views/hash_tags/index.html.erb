<h1 class="page_header">All Hash Tags</h1>

<!--% @hash_tags.each do |hash_tag| %>
	%= link_to "#{hash_tag.name}", hash_tag_path(hash_tag.id), :class => "btn btn-light" %>
end %-->

<% @hash_tags.each do |hash_tag| %>
	<!--%= link_to(hash_tag_path(hash_tag.id)) do %>
			<div class="hash_tag_index_div">
				<span class="hash_tag_link">
					%= hash_tag.name %>
				</span>
				<br>
			  Blog post count: %= hash_tag.blogs.all.count %>
			</div>	
	% end %-->
	<!--a href="#">
		%= hash_tag.name %> 
		<span class="badge">
			%= hash_tag.blogs.all.count %>
		</span>
	</a-->
	<!--%= link_to "##{hash_tag.name}", hash_tag_path(hash_tag.id), :class => 'hash_tag_link' %--> 
	<!--span class="badge"-->
		<% @hashTagCount = hash_tag.blogs.all.count %>
		<% if @hashTagCount == 1 %>
			<%= link_to "##{hash_tag.name}", hash_tag_path(hash_tag.id), :class => 'hash_tag_link hash_tag_link_tier_one' %> 
			<span class="badge" style="font-size: 12px;">
				<%= hash_tag.blogs.all.count %>
			</span>
		<% elsif @hashTagCount == 2 %>
			<%= link_to "##{hash_tag.name}", hash_tag_path(hash_tag.id), :class => 'hash_tag_link hash_tag_link_tier_two' %> 
			<span class="badge" style="font-size: 16px;">
				<%= hash_tag.blogs.all.count %>
			</span>
		<% elsif @hashTagCount == 3 %>
			<%= link_to "##{hash_tag.name}", hash_tag_path(hash_tag.id), :class => 'hash_tag_link hash_tag_link_tier_three' %> 
			<span class="badge" style="font-size: 20px;">
				<%= hash_tag.blogs.all.count %>
			</span>
		<% elsif @hashTagCount == 4 %>
			<%= link_to "##{hash_tag.name}", hash_tag_path(hash_tag.id), :class => 'hash_tag_link hash_tag_link_tier_four' %> 
			<span class="badge" style="font-size: 24px;">
				<%= hash_tag.blogs.all.count %>
			</span>
		<% elsif @hashTagCount == 5 %>
			<%= link_to "##{hash_tag.name}", hash_tag_path(hash_tag.id), :class => 'hash_tag_link hash_tag_link_tier_five' %> 
			<span class="badge" style="font-size: 28px;">
				<%= hash_tag.blogs.all.count %>
			</span>
		<% elsif @hashTagCount == 6 %>
			<%= link_to "##{hash_tag.name}", hash_tag_path(hash_tag.id), :class => 'hash_tag_link hash_tag_link_tier_six' %> 
			<span class="badge" style="font-size: 32px;">
				<%= hash_tag.blogs.all.count %>
			</span>
		<% end %>
	<!--/span-->
	&nbsp;&nbsp;&nbsp;
<% end %>


